# SPEC-101-6 数据库文档

> 模块：用户画像模块 | 服务：`globehive-base:8300` | 版本：v1.0 | 日期：2026-03-29

---

## 1. 涉及数据表总览

| 表名 | 所属 | 说明 | 与本模块关系 |
|------|------|------|------------|
| `ai_user_profile` | GlobeHive | 用户画像主表 | **核心主表** |
| `ai_chat_user_relation` | GlobeHive | 渠道绑定关联表 | ONE ID 合并时迁移渠道关联 |
| `ai_audit_log` | GlobeHive | 审计日志表 | 画像变更、ONE ID 合并写入审计 |
| `blade_user` | BladeX | 平台用户账号 | `user_id` 来源 |
| `blade_tenant` | BladeX | 租户表 | `tenant_id` 来源 |

---

## 2. 主表：ai_user_profile

### 2.1 完整 DDL

```sql
CREATE TABLE `ai_user_profile` (
  -- ── BladeX BaseEntity 标准字段 ──────────────────────────────────
  `id`               bigint       NOT NULL            COMMENT '主键（雪花算法）',
  `tenant_id`        varchar(12)  DEFAULT '000000'    COMMENT '租户ID（关联 blade_tenant.tenant_id）',
  `create_user`      bigint       DEFAULT NULL        COMMENT '创建人（关联 blade_user.id）',
  `create_dept`      bigint       DEFAULT NULL        COMMENT '创建部门',
  `create_time`      datetime     DEFAULT NULL        COMMENT '创建时间',
  `update_user`      bigint       DEFAULT NULL        COMMENT '最后修改人',
  `update_time`      datetime     DEFAULT NULL        COMMENT '最后修改时间',
  `status`           int          DEFAULT 1           COMMENT '状态：1=启用，0=禁用，2=已合并',
  `is_deleted`       int          DEFAULT 0           COMMENT '软删除：0=正常，1=已删',
  -- ── 基础信息 ────────────────────────────────────────────────────
  `user_id`          bigint       NOT NULL            COMMENT '业务用户ID（关联 blade_user.id）',
  `real_name`        varchar(50)  DEFAULT NULL        COMMENT '真实姓名',
  `gender`           tinyint      DEFAULT NULL        COMMENT '性别：0=未知，1=男，2=女',
  `birthday`         date         DEFAULT NULL        COMMENT '生日',
  `constellation`    varchar(20)  DEFAULT NULL        COMMENT '星座',
  `profession`       varchar(50)  DEFAULT NULL        COMMENT '职业',
  `income_range`     varchar(30)  DEFAULT NULL        COMMENT '收入范围',
  -- ── 扩展信息（JSON） ────────────────────────────────────────────
  `family_members`   json         DEFAULT NULL        COMMENT '家庭成员（JSON 数组）',
  `anniversaries`    json         DEFAULT NULL        COMMENT '纪念日列表（JSON 数组）',
  `addresses`        json         DEFAULT NULL        COMMENT '收货地址列表（JSON 数组）',
  `social_accounts`  json         DEFAULT NULL        COMMENT '社交账号（JSON 数组）',
  `preferences`      json         DEFAULT NULL        COMMENT '偏好设置（JSON 对象）',
  `tags`             json         DEFAULT NULL        COMMENT '标签列表（JSON 字符串数组）',
  -- ── 画像统计 ────────────────────────────────────────────────────
  `completeness`     int          DEFAULT 0           COMMENT '画像完整度（0-100）',
  `merged_into`      bigint       DEFAULT NULL        COMMENT '合并目标账号ID（status=2时有值）',
  -- ── 主键与索引 ──────────────────────────────────────────────────
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_tenant_user` (`tenant_id`, `user_id`),
  KEY `idx_tenant_id` (`tenant_id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_create_time` (`create_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='用户画像主表';
```

### 2.2 关键字段说明

| 字段 | 类型 | 特殊说明 |
|------|------|----------|
| `id` | bigint | 雪花算法，**非** AUTO_INCREMENT，Java `@TableId(ASSIGN_ID)` |
| `status` | int | 1=正常，0=禁用，**2=已被 ONE ID 合并** |
| `tags` | json | MyBatis 用 `JacksonTypeHandler` 映射，存储 `["VIP","活跃"]` |
| `family_members` | json | 存储 `[{"name":"张妻","relation":"配偶"}]` |
| `completeness` | int | 每次 update 由 Service 重算，0-100 |

---

## 3. SQL 文件

| 文件 | 说明 |
|------|------|
| [`sql/SPEC-101-ddl.sql`](./sql/SPEC-101-ddl.sql) | 建表 DDL（`ai_user_profile`）|

> 执行顺序：直接在 `globehive` 数据库执行即可，无依赖前置脚本。

---

## 5. 数据变更记录

| 日期 | 变更 | 原因 |
|------|------|------|
| 2026-03-29 | 初始建表 | v1.0 |
| 2026-03-30 | `status` 新增值 2（已合并） | ONE ID 合并功能 |

---

## 6. 诊断 SQL

```sql
-- 查看某租户的用户画像数量
SELECT COUNT(*) FROM ai_user_profile WHERE tenant_id='000000' AND is_deleted=0;

-- 查看画像完整度分布
SELECT completeness, COUNT(*) cnt
FROM ai_user_profile WHERE is_deleted=0
GROUP BY completeness ORDER BY completeness;

-- 查看已被合并的账号
SELECT id, user_id, merged_into, update_time
FROM ai_user_profile WHERE status=2;

-- 检查 JSON 字段是否正常（非 null 且非空串）
SELECT id, JSON_TYPE(tags) tag_type, JSON_ARRAY_LENGTH(tags) tag_count
FROM ai_user_profile WHERE tags IS NOT NULL LIMIT 10;
```

---

## 版本历史

| 版本 | 日期 | 作者 | 说明 |
|------|------|------|------|
| v1.0 | 2026-03-29 | Cascade | 初始建表：基础字段 + 6 个 JSON 字段 + 完整度 |
